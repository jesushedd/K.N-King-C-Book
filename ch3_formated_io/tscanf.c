#include <stdio.h>

int main(int argc, char const *argv[])
{
    int x, y, z;
    float i, j;
    scanf("%d %d %f %f\n", &x, &y, &i, &j);
    
    printf("%d\t%d\t%f\t%f\t%d", x, y, i, j);
    return 0;
}
