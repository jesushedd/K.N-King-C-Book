#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a,b,c,d;
    float i,j,k,l;

    printf("%d, %d, %d, %d\n", a, b, c, d);
    printf("%f. %f, %f, %f\n", i, j, k, l);
    return 0;
}
